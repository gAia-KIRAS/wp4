pyyaml==6.0
tensorflow==2.12.0      # Not required if nci/conv_lib == torch
paramiko==3.2.0
pandas==2.0.3
mlflow==2.3.2           # Will probably be removed
pyinstrument==4.4.0
geopandas==0.13.2
gdal==3.5.1
pytest==7.4.0
torch==2.0.1            # Required when execute/where == server
```